<!-- < % doesn't have equals because we don't want to print that out -->
<!-- checking if value isn't nil, because then the flash has expired -->
<% if flash[:notice] %>
    <div class="alert alert-info mt-4" role="alert">
        <%= flash[:notice] %>
    </div>
<% end %>
