<nav class="navbar navbar-expand-lg shadow-sm" style="background: linear-gradient(to right, #2196F3, #64B5F6);">
  <div class="container">
    <!-- Brand Name -->
    <%= link_to "MyApp", root_path, class: "navbar-brand text-white fw-bold fs-4" %>

    <!-- Mobile Toggle Button -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link text-white fw-semibold px-3" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link text-white fw-semibold px-3" %>
        </li>
      </ul>

      <!-- Right-Aligned Links -->
      <ul class="navbar-nav ms-auto">
        <% if Current.user %>
          <% if Current.user.is_admin %>
            <li class="nav-item">
              <%= link_to "Admin Panel", admin_dashboard_path, class: "nav-link text-warning fw-semibold px-3" %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Projects", projects_path, class: "nav-link text-white fw-semibold px-3" %>
          </li>
          <li class="nav-item">
            <%= link_to Current.user.email, user_profile_path, class: "nav-link text-white fw-semibold px-3" %>
          </li>
          <li class="nav-item">
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-light fw-semibold px-3 mx-2" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", sign_in_path, class: "nav-link text-white fw-semibold px-3" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", sign_up_path, class: "btn btn-outline-light fw-semibold px-3 mx-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
